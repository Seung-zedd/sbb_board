
spring:
  config:
    activate:
      on-profile: profile2
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${MYSQL_URL}
    username: csz0209@sbb3-server
    password: ${MYSQL_PASSWORD}

